<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Set Election Period</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gradient-primary min-vh-100">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8 col-xl-6">
        <div class="card shadow-lg-custom border-radius-xl fade-in-up">
          <div class="card-header bg-gradient-primary text-white text-center py-4">
            <h2 class="h3 fw-bold mb-0">
              <i class="fas fa-calendar-alt me-2"></i>Set Election Period
            </h2>
            <p class="mb-0 small">Configure the election start and end dates</p>
          </div>
          <div class="card-body p-4">
            <div class="alert alert-info">
              <strong>Current Status:</strong> Voting is <%= isVotingOpen ? 'OPEN' : 'CLOSED' %>
            </div>

            <form action="/admin/toggle" method="POST">
              <div class="row">
                <div class="col-md-6 mb-3">
                  <label for="election_start_date" class="form-label">
                    <i class="fas fa-play-circle me-2"></i>Election Start Date & Time
                  </label>
                  <input type="datetime-local" class="form-control" id="election_start_date" name="election_start_date"
                         value="<%= electionStartDate %>" required>
                </div>
                <div class="col-md-6 mb-3">
                  <label for="election_end_date" class="form-label">
                    <i class="fas fa-stop-circle me-2"></i>Election End Date & Time
                  </label>
                  <input type="datetime-local" class="form-control" id="election_end_date" name="election_end_date"
                         value="<%= electionEndDate %>" required>
                </div>
              </div>
              <div class="d-grid">
                <button type="submit" class="btn btn-success btn-lg">
                  <i class="fas fa-save me-2"></i>Set Election Period
                </button>
              </div>
            </form>
            <form action="/admin/toggle/close" method="POST" class="mt-3">
              <div class="d-grid">
                <button type="submit" class="btn btn-danger btn-lg" onclick="return confirm('Are you sure you want to close the election period? This will clear the current election dates.')">
                  <i class="fas fa-times-circle me-2"></i>Close Election Period
                </button>
              </div>
            </form>
            <div class="text-center mt-4">
              <a href="/admin/login" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left me-2"></i>Back to Admin Panel
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
