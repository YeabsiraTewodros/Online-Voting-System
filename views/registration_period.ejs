<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Set Registration Period - Ethiopian Voting System</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/registration_period.css">
</head>
<body>
    <div class="container">
        <h1>Set Voter Registration Period</h1>
        <p>Configure the time period during which voter registration is allowed.</p>

        <form action="/admin/registration-period" method="POST">
            <div class="form-group">
                <label for="registration_start_date">Registration Start Date & Time:</label>
                <input type="datetime-local" id="registration_start_date" name="registration_start_date"
                       value="<%= registrationStartDate %>" required>
            </div>

            <div class="form-group">
                <label for="registration_end_date">Registration End Date & Time:</label>
                <input type="datetime-local" id="registration_end_date" name="registration_end_date"
                       value="<%= registrationEndDate %>" required>
            </div>

            <button type="submit" class="btn">Set Registration Period</button>
        </form>

        <form action="/admin/registration-period/reset" method="POST" class="mt-3">
            <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to reset the registration period? This will clear the current registration dates.')">Reset Registration Period</button>
        </form>

        <a href="/admin/login" class="back-link">← Back to Admin Panel</a>
    </div>
</body>
</html>
